// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/cloudreve/Cloudreve/v4/ent/aclentry"
	"github.com/cloudreve/Cloudreve/v4/ent/file"
	"github.com/cloudreve/Cloudreve/v4/pkg/boolset"
)

// AclEntry is the model entity for the AclEntry schema.
type AclEntry struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// FileID holds the value of the "file_id" field.
	FileID int `json:"file_id,omitempty"`
	// SubjectType holds the value of the "subject_type" field.
	SubjectType aclentry.SubjectType `json:"subject_type,omitempty"`
	// SubjectID holds the value of the "subject_id" field.
	SubjectID int `json:"subject_id,omitempty"`
	// Permissions holds the value of the "permissions" field.
	Permissions *boolset.BooleanSet `json:"permissions,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AclEntryQuery when eager-loading is set.
	Edges        AclEntryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AclEntryEdges holds the relations/edges for other nodes in the graph.
type AclEntryEdges struct {
	// File holds the value of the file edge.
	File *File `json:"file,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// FileOrErr returns the File value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AclEntryEdges) FileOrErr() (*File, error) {
	if e.loadedTypes[0] {
		if e.File == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: file.Label}
		}
		return e.File, nil
	}
	return nil, &NotLoadedError{edge: "file"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AclEntry) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case aclentry.FieldPermissions:
			values[i] = new(boolset.BooleanSet)
		case aclentry.FieldID, aclentry.FieldFileID, aclentry.FieldSubjectID:
			values[i] = new(sql.NullInt64)
		case aclentry.FieldSubjectType:
			values[i] = new(sql.NullString)
		case aclentry.FieldCreatedAt, aclentry.FieldUpdatedAt, aclentry.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AclEntry fields.
func (ae *AclEntry) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case aclentry.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ae.ID = int(value.Int64)
		case aclentry.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ae.CreatedAt = value.Time
			}
		case aclentry.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ae.UpdatedAt = value.Time
			}
		case aclentry.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				ae.DeletedAt = new(time.Time)
				*ae.DeletedAt = value.Time
			}
		case aclentry.FieldFileID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field file_id", values[i])
			} else if value.Valid {
				ae.FileID = int(value.Int64)
			}
		case aclentry.FieldSubjectType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field subject_type", values[i])
			} else if value.Valid {
				ae.SubjectType = aclentry.SubjectType(value.String)
			}
		case aclentry.FieldSubjectID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field subject_id", values[i])
			} else if value.Valid {
				ae.SubjectID = int(value.Int64)
			}
		case aclentry.FieldPermissions:
			if value, ok := values[i].(*boolset.BooleanSet); !ok {
				return fmt.Errorf("unexpected type %T for field permissions", values[i])
			} else if value != nil {
				ae.Permissions = value
			}
		default:
			ae.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AclEntry.
// This includes values selected through modifiers, order, etc.
func (ae *AclEntry) Value(name string) (ent.Value, error) {
	return ae.selectValues.Get(name)
}

// QueryFile queries the "file" edge of the AclEntry entity.
func (ae *AclEntry) QueryFile() *FileQuery {
	return NewAclEntryClient(ae.config).QueryFile(ae)
}

// Update returns a builder for updating this AclEntry.
// Note that you need to call AclEntry.Unwrap() before calling this method if this AclEntry
// was returned from a transaction, and the transaction was committed or rolled back.
func (ae *AclEntry) Update() *AclEntryUpdateOne {
	return NewAclEntryClient(ae.config).UpdateOne(ae)
}

// Unwrap unwraps the AclEntry entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ae *AclEntry) Unwrap() *AclEntry {
	_tx, ok := ae.config.driver.(*txDriver)
	if !ok {
		panic("ent: AclEntry is not a transactional entity")
	}
	ae.config.driver = _tx.drv
	return ae
}

// String implements the fmt.Stringer.
func (ae *AclEntry) String() string {
	var builder strings.Builder
	builder.WriteString("AclEntry(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ae.ID))
	builder.WriteString("created_at=")
	builder.WriteString(ae.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ae.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := ae.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("file_id=")
	builder.WriteString(fmt.Sprintf("%v", ae.FileID))
	builder.WriteString(", ")
	builder.WriteString("subject_type=")
	builder.WriteString(fmt.Sprintf("%v", ae.SubjectType))
	builder.WriteString(", ")
	builder.WriteString("subject_id=")
	builder.WriteString(fmt.Sprintf("%v", ae.SubjectID))
	builder.WriteString(", ")
	builder.WriteString("permissions=")
	builder.WriteString(fmt.Sprintf("%v", ae.Permissions))
	builder.WriteByte(')')
	return builder.String()
}

// SetFile manually set the edge as loaded state.
func (e *AclEntry) SetFile(v *File) {
	e.Edges.File = v
	e.Edges.loadedTypes[0] = true
}

// AclEntries is a parsable slice of AclEntry.
type AclEntries []*AclEntry
