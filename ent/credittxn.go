// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/cloudreve/Cloudreve/v4/ent/credittxn"
	"github.com/cloudreve/Cloudreve/v4/ent/user"
)

// CreditTxn is the model entity for the CreditTxn schema.
type CreditTxn struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// Amount holds the value of the "amount" field.
	Amount int64 `json:"amount,omitempty"`
	// Type holds the value of the "type" field.
	Type credittxn.Type `json:"type,omitempty"`
	// Ref holds the value of the "ref" field.
	Ref string `json:"ref,omitempty"`
	// Des holds the value of the "des" field.
	Des string `json:"des,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CreditTxnQuery when eager-loading is set.
	Edges        CreditTxnEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CreditTxnEdges holds the relations/edges for other nodes in the graph.
type CreditTxnEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CreditTxnEdges) UserOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.User == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CreditTxn) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case credittxn.FieldID, credittxn.FieldUserID, credittxn.FieldAmount:
			values[i] = new(sql.NullInt64)
		case credittxn.FieldType, credittxn.FieldRef, credittxn.FieldDes:
			values[i] = new(sql.NullString)
		case credittxn.FieldCreatedAt, credittxn.FieldUpdatedAt, credittxn.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CreditTxn fields.
func (ct *CreditTxn) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case credittxn.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ct.ID = int(value.Int64)
		case credittxn.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ct.CreatedAt = value.Time
			}
		case credittxn.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ct.UpdatedAt = value.Time
			}
		case credittxn.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				ct.DeletedAt = new(time.Time)
				*ct.DeletedAt = value.Time
			}
		case credittxn.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				ct.UserID = int(value.Int64)
			}
		case credittxn.FieldAmount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field amount", values[i])
			} else if value.Valid {
				ct.Amount = value.Int64
			}
		case credittxn.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				ct.Type = credittxn.Type(value.String)
			}
		case credittxn.FieldRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ref", values[i])
			} else if value.Valid {
				ct.Ref = value.String
			}
		case credittxn.FieldDes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field des", values[i])
			} else if value.Valid {
				ct.Des = value.String
			}
		default:
			ct.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CreditTxn.
// This includes values selected through modifiers, order, etc.
func (ct *CreditTxn) Value(name string) (ent.Value, error) {
	return ct.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the CreditTxn entity.
func (ct *CreditTxn) QueryUser() *UserQuery {
	return NewCreditTxnClient(ct.config).QueryUser(ct)
}

// Update returns a builder for updating this CreditTxn.
// Note that you need to call CreditTxn.Unwrap() before calling this method if this CreditTxn
// was returned from a transaction, and the transaction was committed or rolled back.
func (ct *CreditTxn) Update() *CreditTxnUpdateOne {
	return NewCreditTxnClient(ct.config).UpdateOne(ct)
}

// Unwrap unwraps the CreditTxn entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ct *CreditTxn) Unwrap() *CreditTxn {
	_tx, ok := ct.config.driver.(*txDriver)
	if !ok {
		panic("ent: CreditTxn is not a transactional entity")
	}
	ct.config.driver = _tx.drv
	return ct
}

// String implements the fmt.Stringer.
func (ct *CreditTxn) String() string {
	var builder strings.Builder
	builder.WriteString("CreditTxn(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ct.ID))
	builder.WriteString("created_at=")
	builder.WriteString(ct.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ct.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := ct.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", ct.UserID))
	builder.WriteString(", ")
	builder.WriteString("amount=")
	builder.WriteString(fmt.Sprintf("%v", ct.Amount))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", ct.Type))
	builder.WriteString(", ")
	builder.WriteString("ref=")
	builder.WriteString(ct.Ref)
	builder.WriteString(", ")
	builder.WriteString("des=")
	builder.WriteString(ct.Des)
	builder.WriteByte(')')
	return builder.String()
}

// SetUser manually set the edge as loaded state.
func (e *CreditTxn) SetUser(v *User) {
	e.Edges.User = v
	e.Edges.loadedTypes[0] = true
}

// CreditTxns is a parsable slice of CreditTxn.
type CreditTxns []*CreditTxn
