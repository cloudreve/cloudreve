// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/cloudreve/Cloudreve/v4/ent/ssobinding"
	"github.com/cloudreve/Cloudreve/v4/ent/user"
)

// SsoBinding is the model entity for the SsoBinding schema.
type SsoBinding struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// Provider holds the value of the "provider" field.
	Provider string `json:"provider,omitempty"`
	// Subject holds the value of the "subject" field.
	Subject string `json:"subject,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SsoBindingQuery when eager-loading is set.
	Edges        SsoBindingEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SsoBindingEdges holds the relations/edges for other nodes in the graph.
type SsoBindingEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SsoBindingEdges) UserOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.User == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SsoBinding) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ssobinding.FieldID, ssobinding.FieldUserID:
			values[i] = new(sql.NullInt64)
		case ssobinding.FieldProvider, ssobinding.FieldSubject:
			values[i] = new(sql.NullString)
		case ssobinding.FieldCreatedAt, ssobinding.FieldUpdatedAt, ssobinding.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SsoBinding fields.
func (sb *SsoBinding) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ssobinding.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sb.ID = int(value.Int64)
		case ssobinding.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sb.CreatedAt = value.Time
			}
		case ssobinding.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sb.UpdatedAt = value.Time
			}
		case ssobinding.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				sb.DeletedAt = new(time.Time)
				*sb.DeletedAt = value.Time
			}
		case ssobinding.FieldProvider:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[i])
			} else if value.Valid {
				sb.Provider = value.String
			}
		case ssobinding.FieldSubject:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field subject", values[i])
			} else if value.Valid {
				sb.Subject = value.String
			}
		case ssobinding.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				sb.UserID = int(value.Int64)
			}
		default:
			sb.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SsoBinding.
// This includes values selected through modifiers, order, etc.
func (sb *SsoBinding) Value(name string) (ent.Value, error) {
	return sb.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the SsoBinding entity.
func (sb *SsoBinding) QueryUser() *UserQuery {
	return NewSsoBindingClient(sb.config).QueryUser(sb)
}

// Update returns a builder for updating this SsoBinding.
// Note that you need to call SsoBinding.Unwrap() before calling this method if this SsoBinding
// was returned from a transaction, and the transaction was committed or rolled back.
func (sb *SsoBinding) Update() *SsoBindingUpdateOne {
	return NewSsoBindingClient(sb.config).UpdateOne(sb)
}

// Unwrap unwraps the SsoBinding entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sb *SsoBinding) Unwrap() *SsoBinding {
	_tx, ok := sb.config.driver.(*txDriver)
	if !ok {
		panic("ent: SsoBinding is not a transactional entity")
	}
	sb.config.driver = _tx.drv
	return sb
}

// String implements the fmt.Stringer.
func (sb *SsoBinding) String() string {
	var builder strings.Builder
	builder.WriteString("SsoBinding(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sb.ID))
	builder.WriteString("created_at=")
	builder.WriteString(sb.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sb.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := sb.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("provider=")
	builder.WriteString(sb.Provider)
	builder.WriteString(", ")
	builder.WriteString("subject=")
	builder.WriteString(sb.Subject)
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", sb.UserID))
	builder.WriteByte(')')
	return builder.String()
}

// SetUser manually set the edge as loaded state.
func (e *SsoBinding) SetUser(v *User) {
	e.Edges.User = v
	e.Edges.loadedTypes[0] = true
}

// SsoBindings is a parsable slice of SsoBinding.
type SsoBindings []*SsoBinding
